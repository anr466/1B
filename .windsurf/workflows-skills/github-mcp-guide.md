---
description: دليل استخدام GitHub MCP مع نظام التداول الذكي
---

# 🐙 دليل GitHub MCP Integration

## 📋 نظرة عامة
تم تكامل GitHub MCP بنجاح مع مشروع التداول الذكي. يوفر هذا التكامل إمكانيات متقدمة للتفاعل مع GitHub APIs ومستودعات الكود.

## ⚙️ الإعدادات المطبقة

### GitHub MCP Server
```json
"github": {
  "args": ["-y", "@modelcontextprotocol/server-github"],
  "command": "npx",
  "env": {
    "GITHUB_TOKEN": "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"
  }
}
```

### التوكن المستخدم
- **المصدر**: `.env` الملف في السطر 64
- **النطاق**: `GITHUB_TOKEN=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`
- **الحساب**: `anr466` (ID: 60723216)

## 🔧 الميزات المتاحة

### 1. إدارة المستودعات
- **قراءة معلومات المستودع**
- **استعراض الملفات والمجلدات**
- **البحث في الكود**
- **عرض تاريخ الـ commits**

### 2. إدارة Issues و Pull Requests
- **إنشاء وتعديل Issues**
- **مراجعة Pull Requests**
- **إضافة تعليقات**
- **تتبع حالة التطوير**

### 3. تحليل المشروع
- **إحصائيات المساهمين**
- **تحليل جودة الكود**
- **مراقبة النشاط**
- **تتبع الأخطاء والإصلاحات**

## 🚀 الاستخدام العملي

### أمثلة للاستخدام مع المشروع:

#### 1. تحليل تطور النظام
```
يمكنك الآن سؤال المساعد عن:
- ما هي آخر التحديثات على نظام التداول؟
- كم عدد الـ commits في الشهر الماضي؟
- ما هي الأخطاء المفتوحة حالياً؟
```

#### 2. مراجعة الكود
```
- فحص جودة الكود في backend/strategies/
- تحليل الأداء في mobile_app/
- مراجعة التحسينات المطلوبة
```

#### 3. التوثيق التلقائي
```
- إنشاء documentation للميزات الجديدة
- تحديث README.md تلقائياً
- إنشاء CHANGELOG للإصدارات
```

## 🔒 الأمان والخصوصية

### حماية التوكن
- ✅ التوكن محفوظ في متغير بيئة منفصل
- ✅ لا يتم كشفه في الكود المصدري
- ✅ يمكن تجديده بسهولة عند الحاجة

### الصلاحيات
- **القراءة**: الوصول لمعلومات المستودعات العامة والخاصة
- **الكتابة**: إنشاء Issues وPull Requests (حسب صلاحيات التوكن)
- **المستودعات**: `anr466/*` repositories

## 🔄 التكامل مع MCP الحالية

### عدم التعارض مع الأنظمة الموجودة:
- ✅ **deepwiki**: يعمل بانسجام مع GitHub MCP
- ✅ **filesystem**: تكامل محلي منفصل
- ✅ **figma-remote**: لا يتداخل مع GitHub
- ✅ **playwright/puppeteer**: أدوات منفصلة
- ✅ **sequential-thinking**: يستفيد من بيانات GitHub
- ✅ **time**: مستقل تماماً

## 🛠️ استكشاف الأخطاء

### المشاكل الشائعة:

#### 1. خطأ Authentication
```bash
# التحقق من صحة التوكن
curl -H "Authorization: token TOKEN_HERE" https://api.github.com/user
```

#### 2. خطأ Package Deprecated  
```
تحذير: @modelcontextprotocol/server-github@2025.4.8 deprecated
الحل: النظام يعمل حالياً، ننتظر التحديث الرسمي
```

#### 3. مشكلة الاتصال
```bash
# فحص npm والاتصال
npm list -g @modelcontextprotocol/server-github
```

## 📈 التحسينات المستقبلية

### خطط التطوير:
1. **ربط مع نظام التنبيهات**: إشعار عند حدوث commits جديدة
2. **تحليل الأداء التلقائي**: مراقبة أداء النظام من خلال Git logs
3. **نشر تلقائي**: ربط مع CI/CD للمشروع
4. **إحصائيات متقدمة**: تقارير دورية عن تطور المشروع

## 📞 الدعم والمساعدة

### عند الحاجة للمساعدة:
1. **التحقق من الإعدادات**: `~/.codeium/windsurf/mcp_config.json`
2. **النسخة الاحتياطية متوفرة**: `mcp_config.json.backup`
3. **استعادة الإعدادات السابقة**: نسخ من الملف الاحتياطي
4. **تحديث التوكن**: تعديل في `.env` السطر 64

---
*تم إنشاء هذا الدليل في: فبراير 2026*
*حالة التكامل: ✅ مفعل ويعمل*
